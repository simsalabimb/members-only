<div class = "posts">
<%= form_for @post do |f| %>

  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label :Post %>
  <%= f.text_area :body, size: "30x6" %><br />

  <%= f.submit "Create" %>
<% end %>

</div>
